- name: abc
  block:
  - name: test
    command: whoami
    register: info
  - name: debug
    debug: 
      msg: "{{info}}"
  tags:
  - register

# 想要获取 ansible_facts 变量，需要在 Role 启动文件中，将 gather_facts 字段设为 yes。
- name: debug
  debug: 
    msg: "{{ansible_facts}}"
  tags:
  - facts
